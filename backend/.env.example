# Rails Configuration
RAILS_ENV=development
RAILS_MAX_THREADS=5

# Database Configuration (matches docker-compose.yml services)
DB_HOST=db
DB_USERNAME=app
DB_PASSWORD=app_password
DB_NAME=careermap_poc_development
DB_PORT=3306

# JWT Secret (for development)
JWT_SECRET=dev-jwt-secret-change-me